name: Deploy

# Separate from ci.yml: this is the only workflow that carries the Cloudflare
# secrets, so third-party audit tools (Lighthouse, axe, etc.) in ci.yml's jobs
# never run in a job that has them.
#
# Every environment splits a build job (no secrets, caches allowed) from a
# deploy job (the only place the token lives). The deploy job doesn't share a
# VM with Astro/Vite/Tailwind/Mermaid/Chromium, project dependencies, install
# scripts, or any cache ci.yml writes to — it checks out only wrangler.jsonc,
# installs wrangler in isolation with scripts off, and takes dist/ as an
# artifact. See each deploy job's comments for why.
on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  # Distinctive ids: "build" and "check" already exist in ci.yml, and
  # required status checks are matched by check-run name, which defaults to
  # the job id when a job has no `name:`.
  preview-build:
    # Same-repo PRs only — a fork's workflow content never runs with the
    # Cloudflare secrets (the deploy job's token isn't reachable from this
    # job at all, but the artifact it produces is, so gate here too).
    #
    # Dependabot PRs pass the same-repo check (its branches live in this
    # repo), but GitHub gives a Dependabot-triggered run only Dependabot
    # secrets — none exist for this repo — and a read-only GITHUB_TOKEN, so
    # a Dependabot run could never authenticate to Cloudflare anyway. Skip
    # it explicitly instead of letting it fail: a skipped job reports
    # Success, so it doesn't block a security-update PR the way a failed
    # required check would, and re-running as Dave (the only way to change
    # the actor) means reviewing the bump first.
    # https://docs.github.com/en/code-security/dependabot/troubleshooting-dependabot/troubleshooting-dependabot-on-github-actions
    if: >-
      github.event_name == 'pull_request' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      # Mermaid diagrams render in headless Chromium at build, same as ci.yml.
      - uses: ./.github/actions/setup
        with:
          browser: "true"
      # No PUBLIC_CF_WEB_ANALYTICS_TOKEN here, intentionally: previews must
      # not carry the production analytics beacon. astro:env leaves it
      # undefined when unset, so the build just ships without it (the
      # production build job is the only one that sets it).
      - run: pnpm build
      # No job output carries anything from here to preview-deploy besides
      # this artifact — deliberately. This job runs pnpm install (with
      # allowBuilds scripts), Astro, Vite, Tailwind, sharp, and everything
      # else `pnpm build` pulls in; any of that could write to $GITHUB_OUTPUT
      # itself (a job output is just a line appended to a file any process
      # in the job can reach, on a hosted runner with passwordless sudo).
      # preview-deploy used to trust a wrangler-version output from this
      # job; it no longer does (see preview-deploy) — a poisoned output
      # here could otherwise have pointed `npm install` at an arbitrary
      # package, git spec, or tarball, which would then run with
      # CLOUDFLARE_API_TOKEN present.
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: preview-dist
          path: dist/
          retention-days: 1

  preview-deploy:
    needs: preview-build
    # A skipped `needs` job already skips this one by default (no explicit
    # `if` required for that). Repeating the same-repo/Dependabot condition
    # here anyway is deliberate belt-and-suspenders: this job, not
    # preview-build, is the one that actually holds the Cloudflare token, so
    # its own gate shouldn't depend on nobody ever adding `if: always()` (or
    # similar) to loosen it later.
    if: >-
      github.event_name == 'pull_request' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    concurrency:
      group: preview-${{ github.event.pull_request.number }}
      cancel-in-progress: true
    environment:
      name: Preview
      url: ${{ steps.deploy.outputs.url }}
    steps:
      # wrangler.jsonc, for wrangler to read, and pnpm-lock.yaml, so this job
      # can read the pinned wrangler version from its OWN checkout instead of
      # trusting preview-build's output (see the version step below for why).
      # No project code, no source. persist-credentials: false, since nothing
      # after this needs git auth.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          sparse-checkout: |
            wrangler.jsonc
            pnpm-lock.yaml
          sparse-checkout-cone-mode: false
          persist-credentials: false
      # L2: the only thing that separates this job's --env="" upload from a
      # production deploy is that the top-level config has no route/routes.
      # Fail closed if that ever changes, rather than silently deploying
      # production traffic from a PR run. (wrangler.jsonc is JSONC; strip
      # `//` line comments before parsing — this file has none inside string
      # values.)
      - name: Refuse a top-level route or routes key
        run: |
          set -eu
          json="$(sed -E 's#//.*$##' wrangler.jsonc)"
          if echo "$json" | jq -e 'has("route") or has("routes")' >/dev/null; then
            echo "::error::wrangler.jsonc has a top-level route or routes key. Preview deploys the default target with --env=\"\" and must never carry a custom domain or zone route."
            exit 1
          fi
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: preview-dist
          path: dist/
      # The artifact only ever lands in dist/ (the `path:` above), and
      # wrangler reads dotenv files from the working directory, not from
      # dist/ — but assert it anyway, in case a future change moves where
      # the artifact unpacks, or a crafted artifact tries a path trick.
      - name: Refuse a stray dotenv-like file at the job root
        run: |
          set -eu
          shopt -s nullglob
          stray=(.env* .dev.vars*)
          shopt -u nullglob
          if [ "${#stray[@]}" -gt 0 ]; then
            echo "::error::Unexpected dotenv-like file(s) before wrangler runs: ${stray[*]}"
            exit 1
          fi
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
      # Not ./.github/actions/setup: that installs the whole project
      # (pnpm install, allowBuilds scripts, Playwright/Chromium) from a
      # cache ci.yml jobs write to. This installs only wrangler, from npm
      # directly, with install scripts off, into a scratch directory outside
      # the checkout — nothing here can plant a fake wrangler binary or
      # touch project code, and nothing here restores or writes a cache.
      - name: Install wrangler (version from this checkout, verified, scripts off, outside the checkout)
        run: |
          set -eu

          # The version comes from THIS job's own sparse checkout of
          # pnpm-lock.yaml, never from preview-build's job output.
          # preview-build runs the full dependency/build toolchain (pnpm
          # install with allowBuilds scripts, Astro, Vite, Tailwind, sharp);
          # any of that — including a compromised dependency, on a hosted
          # runner with passwordless sudo — can write a job output. Trusting
          # a version string from there would let a poisoned build point
          # this install at an arbitrary package, a git spec, or a tarball
          # URL, which would then run with CLOUDFLARE_API_TOKEN present in
          # the next step. This job trusts nothing from preview-build except
          # the static files in the dist/ artifact.
          #
          # Extraction is a fixed grep/sed on pnpm-lock.yaml's stable,
          # machine-generated shape, not a general YAML parser: the
          # importers' top-level "wrangler:" entry is 6-space indented and
          # unique (the packages: section's entries are the different
          # string "wrangler@4.136.3:"). Whatever comes out still has to
          # pass the strict x.y.z regex below, so a lockfile format change
          # fails closed instead of installing an unvalidated string.
          version="$(grep -A2 -m1 '^      wrangler:$' pnpm-lock.yaml | grep '^        version: ' | sed -E 's/^ {8}version: ([0-9]+\.[0-9]+\.[0-9]+).*/\1/')"
          if ! printf '%s' "$version" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "::error::Could not extract a valid wrangler version (x.y.z) from pnpm-lock.yaml; got: ${version:-<empty>}"
            exit 1
          fi

          # No .npmrc should exist in this sparse checkout (only
          # wrangler.jsonc and pnpm-lock.yaml were checked out), but remove
          # one if it somehow appears: an .npmrc can redirect npm's
          # registry, and --registry below should be the only thing
          # deciding that.
          rm -f .npmrc

          mkdir -p "$RUNNER_TEMP/wrangler"
          npm install --prefix "$RUNNER_TEMP/wrangler" --ignore-scripts --no-audit --no-fund \
            --registry=https://registry.npmjs.org/ "wrangler@$version"
      - name: Deploy preview
        id: deploy
        env:
          # Scoped to this step only: it's the one step in the job that
          # needs them.
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # Explicit, so a .env/.env.local file can't redirect wrangler's API
          # calls: wrangler fills a variable from a dotenv file only when
          # it's not already set in the process environment, and it loads
          # dotenv files from the working directory on every command.
          # Verified against wrangler 4.136.3: with this unset, a
          # CLOUDFLARE_API_BASE_URL in .env.local silently redirects
          # `whoami`'s request; with it set here, the request goes to the
          # real API regardless of what's on disk.
          CLOUDFLARE_API_BASE_URL: https://api.cloudflare.com/client/v4
          WRANGLER_OUTPUT_FILE_PATH: ${{ runner.temp }}/wrangler-output.ndjson
          PR_NUMBER: ${{ github.event.pull_request.number }}
          WRANGLER_BIN: ${{ runner.temp }}/wrangler/node_modules/.bin/wrangler
        run: |
          set -eu

          # --env="" targets the top-level (default/staging) config and
          # silences Wrangler's "multiple environments configured, but none
          # specified" warning — env.production exists in wrangler.jsonc, so
          # this otherwise logs on every PR. Verified with
          # `wrangler versions upload --dry-run`: no warning, exit 0.
          # https://developers.cloudflare.com/workers/wrangler/commands/#versions-upload
          #
          # This uploads a new *version*; it doesn't turn on workers_dev or
          # preview_urls, and it doesn't create the Worker if it doesn't
          # exist yet (`versions upload` requires the Worker exists — see
          # the PR body's merge preconditions). Only `wrangler deploy` (the
          # staging job) or the Cloudflare dashboard change those settings.
          #
          # Alias rules (developers.cloudflare.com/workers/configuration/previews):
          # lowercase letters/numbers/dashes, must start with a letter, and
          # "<alias>-<worker-name>" together must be <= 63 chars (DNS label
          # limit). "pr-<number>" is always a handful of characters, well
          # under the limit combined with the "website" worker name.
          "$WRANGLER_BIN" versions upload --env="" --preview-alias "pr-$PR_NUMBER"

          # No wrangler-action: even a plain `wrangler` invocation like this
          # one needs custom parsing for the alias URL (wrangler-action's
          # `deployment-url`/`pages-deployment-alias-url` outputs don't cover
          # a Workers preview-alias URL), and using it here would mean either
          # letting it install its own copy of wrangler inside this already
          # source-free job or fighting its assumptions about running from a
          # full checkout — neither is simpler than invoking the isolated
          # binary above directly.
          #
          # Read the alias URL from Wrangler's own ND-JSON output file.
          # Confirmed the field name against the installed wrangler@4.136.3
          # source (node_modules/wrangler/wrangler-dist/cli.js): a
          # `{"type":"version-upload", ..., "preview_alias_url": "..."}`
          # record, populated only once the target Worker has Version
          # Preview URLs enabled server-side. `--dry-run` omits both URL
          # fields (no upload happens, so there's no version to preview) —
          # this line runs unverified until a real Cloudflare account
          # exists; see the live-checks list in the PR body.
          url=$(jq -r 'select(.type == "version-upload") | .preview_alias_url // empty' "$WRANGLER_OUTPUT_FILE_PATH" | tail -n1)
          if [ -z "$url" ]; then
            echo "::error::wrangler versions upload did not report a preview_alias_url in WRANGLER_OUTPUT_FILE_PATH"
            exit 1
          fi
          echo "url=$url" >> "$GITHUB_OUTPUT"
